/**
 * ParentOnboarding.jsx
 *
 * Entry point for the tnc.saraswatitutorials.com subdomain.
 * Renders the same rebuilt TermsAndConditions page so both routes
 * (tnc. subdomain and /terms-conditions) stay perfectly in sync.
 */
export { default } from "./TermsAndConditions";
